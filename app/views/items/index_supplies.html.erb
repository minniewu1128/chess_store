<% if @supplies.empty? %>
  <h3>There are no supplies in the system at this time</h3>
<% else %>
  <h3>Available Supplies at the Chess Warehouse</h3>

<%= render partial: "current_items1", locals: {items: @supplies, item_type: "supplies"} %>

<% end %>